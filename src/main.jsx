import React from "react";
import ReactDOM from "react-dom/client";
import PickupApp from "./PickupApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PickupApp />
  </React.StrictMode>
);
